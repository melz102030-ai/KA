# Firestore models are written as plain maps, so no reflection keep-rules are
# needed here. Add app-specific rules below as the codebase grows.
-keepattributes Signature
-keepattributes *Annotation*
