{
  "name": "@akbadna/functions",
  "version": "0.1.0",
  "description": "Firebase Cloud Functions — business logic for Akbadna",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,auth",
    "deploy": "npm run build && firebase deploy --only functions",
    "lint": "eslint src"
  },
  "dependencies": {
    "@akbadna/core": "*",
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "typescript": "^5.7.2"
  }
}
